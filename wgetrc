#ignore robots.txt and <meta name=robots content=nofollow>
#robots=off

#print HTTP and FTP server responses
server_response=on

#retry when connection refused
retry_connrefused=on

#retry if download fails (default is 20)
tries=20

#wait before timing out (60 seconds). Applies to all timeouts: DNS, connect, and read. (default is 15 mins)
timeout=60

#don't go up directory structure when downloading recursively
#no_parent=on

#Use (server-provided) last modification date (if available)
timestamping=on

#add extensions based on file type (e.g. .html to 'text/html', .css to 'text/css')
adjust_extension=on